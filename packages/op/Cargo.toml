[package]
name = "tokimo-vfs-op"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio      = { version = "1", features = ["sync"] }
tokimo-vfs-core = { path = "../core" }

[lints]
workspace = true
